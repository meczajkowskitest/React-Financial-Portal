import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <h1>Welcome to React Financial Portal</h1>
  </React.StrictMode>,
  document.getElementById('root')
);
